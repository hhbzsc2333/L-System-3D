/*#pragma once
#include <string>
#include <unordered_map>

class Grammar {
public:
    std::string start;
    std::string result;
    std::unordered_map<char, std::string> generations;

    void Iteration(const std::unordered_map<char, std::string>& rules, int level);
};
*/